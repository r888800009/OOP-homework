10627108 陳文捷 10612150 林詠翔 資訊二甲

public static void main (String[] args)
{
    Array dice
    int n = 0;
        
    for (0 to 2) // get random number
    {
            n = Random number from 1 to 6
            Add n to dice
    } // for
        
    if (is three of a kind)
        output "three of a kind"
            
    else if (is straight)
		output "straight"
			
    else if (is pair)
		output "pair"
			
	else
		output the sum of dice
            
} // main

void isThreeOfKink(int count)
{
    int first = first element in the array
    for (index from 0 to 2)
    {
        if (array[index != first)
	    return false
	count --
    } // for 
    
    if count == 0
        return true // it is a three of king
	
    else
        return false
    
} // isThreeOfKink

void isPair()
{
    create a bucket {0, 0, 0, 0, 0, 0}
    for (int i : dice) bucket[i - 1]++
    
    for (index from 0 to 5)
    {
        if (bucket[index] == 2)
	    return true
    } // for
} // isPair

boolean isStraight()
{
    sort the array
    Let pre = array[0]
    
    for (index from 1 to 2)
    {
        if (pre + 1 != arrar[index])
	    return false
    } // for
    
    return true
} // isStraight
